{
  "name": "teamkompass",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check outputs/team-manager/app.js && node --check outputs/team-manager/next-level.js && node --check outputs/team-manager/service-worker.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  }
}
